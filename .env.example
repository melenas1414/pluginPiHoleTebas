# Configuración para AntiTebas Plugin

# ==============================================
# CONFIGURACIÓN PI-HOLE
# ==============================================

# Host de Pi-hole (IP o hostname)
PIHOLE_HOST=192.168.1.100

# Puerto de Pi-hole (opcional - por defecto 80)
# Ejemplos: 80 (estándar), 8080 (alternativo), 443 (HTTPS)
PIHOLE_PORT=80

# Puerto DNS de Pi-hole (opcional - por defecto 53)
# Solo cambiar si Pi-hole DNS corre en puerto no estándar
PIHOLE_DNS_PORT=53

# Token API de Pi-hole (opcional para sincronización avanzada)
# Se obtiene en: Pi-hole Admin → Settings → API → Show API Token
PIHOLE_API_TOKEN=

# Usar HTTPS para conectar a Pi-hole (true/false)
PIHOLE_SSL=false

# ==============================================
# CONFIGURACIÓN WARP
# ==============================================

# Host del servidor WARP (IP donde corre el contenedor Docker)
WARP_PROXY_HOST=192.168.1.200

# Puerto del proxy SOCKS5 (por defecto 1080)
WARP_SOCKS_PORT=1080

# Puerto del transparent proxy (por defecto 8080)
WARP_TRANSPARENT_PORT=8080

# Configuración de WARP (opcional para WARP Teams)
WARP_TEAM_ID=
WARP_LICENSE_KEY=

# ==============================================
# CONFIGURACIÓN DEL PLUGIN
# ==============================================

# Intervalo de actualización de listas (en segundos)
UPDATE_INTERVAL=3600

# Modo debug (true/false)
DEBUG_MODE=false

# Configuración de logging
LOG_LEVEL=INFO

# ==============================================
# EJEMPLOS DE CONFIGURACIÓN POR PUERTO
# ==============================================

# Ejemplo 1: Pi-hole en puerto estándar 80
# PIHOLE_HOST=192.168.1.100
# PIHOLE_PORT=80

# Ejemplo 2: Pi-hole en puerto 8080  
# PIHOLE_HOST=192.168.1.100
# PIHOLE_PORT=8080

# Ejemplo 3: Pi-hole con HTTPS en puerto 443
# PIHOLE_HOST=pihole.midominio.com
# PIHOLE_PORT=443
# PIHOLE_SSL=true

# Ejemplo 4: Pi-hole con DNS en puerto custom
# PIHOLE_HOST=192.168.1.100
# PIHOLE_PORT=8080
# PIHOLE_DNS_PORT=5353